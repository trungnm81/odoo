<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="group_corebanking_integration" model="res.groups">
        <field name="name">Core Banking Integration</field>
        <field name="implied_ids" eval="[(4, ref('base.group_user'))]"/>
        <field name="comment">Được phép xử lý staging, log đồng bộ và cập nhật dữ liệu CIF/segment từ Core Banking.</field>
        <field name="user_ids" eval="[(4, ref('base.user_root')), (4, ref('base.user_admin'))]"/>
    </record>

</odoo>
